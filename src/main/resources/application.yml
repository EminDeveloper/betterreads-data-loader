spring:
  data:
    cassandra:
      keyspace-name: main
      username: NBjivLTUomLnZbKoUpZLdLEZ
      password: <secret>
      schema-action: create_if_not_exists
      request:
        timeout: 10s
      connection:
        connect-timeout: 10s
        init-query-timeout: 10s

datastax.astra:
    secure-connect-bundle: secure-connect.zip

astra.db:
    id: <db-id>
    region: asia-south1
    keyspace: main
    application.token: <token>
